{
  "name": "bean-culture-order",
  "version": "1.0.0",
  "private": true,
  "description": "Bean Culture cafe self-order web app connected to Square (dine-in table + takeaway aware)",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "node server/index.js",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
